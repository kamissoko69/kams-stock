import{Router}from"express";import{auth,allow}from"../middleware/authMiddleware.js";import*as c from"../controllers/productController.js";const r=Router();r.use(auth);r.get("/",c.listProducts);r.get("/:id",c.getProduct);r.post("/",allow("ADMIN","MANAGER"),c.createProduct);r.put("/:id",allow("ADMIN","MANAGER"),c.updateProduct);r.delete("/:id",allow("ADMIN"),c.deleteProduct);export default r;